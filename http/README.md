# HTTP Requests

This directory contains ready-to-run HTTP requests for each OWASP example.

You can run them directly from IntelliJ IDEA HTTP Client.

Make sure the application is running before executing requests:

```bash
docker compose up